<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teachapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeachApp">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LayoutActivity" />
        <activity android:name=".LayoutLinearActivity" />
        <activity android:name=".LayoutRelativeActivity" />
        <activity android:name=".ComponentActivity" />
        <activity android:name=".ContentProviderActivity" />
        <activity android:name=".ConversationBoxActivity" />
        <activity android:name=".ConversationExplanationActivity" />
        <activity android:name=".DialogueActivity" />
        <activity android:name=".DynamicCodeActivity" />
        <activity android:name=".IntentActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SharedPreferencesActivity" />

    </application>

</manifest>